# Changelog

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.6.7] - 2026-08-14

### 新增

- **新建会话时选择 Agent 模式**：点「新窗口」「新建 tab」或执行 `DSH: New Session` 命令时，弹出原生选择框，从 DSH 的预设中挑选本轮的模式（标准模式 / 极简模式 / PTC 模式 / 创造模式 等，含显示名与说明，默认项有标注），选完再以该 preset 创建会话。若部署没有配置任何 preset，则退回到配置里的默认 preset，不影响正常开新会话。

## [0.6.6] - 2026-08-14

### 修复

- **回合状态不再卡在「进行中」**：聊天已完整答复但状态一直显示「进行中」的问题已修复。
  - 根因一：本地回合 id 从 0 递增，会和 DSH 的正数轮次号撞车，导致 `turn/end` 匹配到错误的回合。现在本地占位回合与历史重建的孤立消息兜底回合改用**负数 id**，与 DSH 轮次号永不相撞。
  - 根因二：已完成回合的迟到事件可能把新的待接管回合 rekey 到旧轮次号（高水位守卫缺失）。现在只有高于会话高水位 `lastSeenTurn` 的轮次号才会被采纳，且高水位只统计真实的 DSH 轮次号，不再被历史兜底 id 污染。
  - 根因三：兜底结束定时器会无条件清掉 `currentTurnId`/`pendingTurnId`，误伤在窗口期内启动的新回合。现在 `turn/end` 直接按权威的 DSH 轮次号匹配回合，定时器也只清理仍属于自己的运行指针。
- **选中内容 / 当前文件能正确附带**：聊天面板获得焦点时 `window.activeTextEditor` 会变成 `undefined`，导致选中内容静默丢失、agent 误以为自己「跟编辑器没有连接」。现在：
  - 在选区产生时就记住「当前文件 + 最新非空选区」（跨越焦点变化）；
  - 发送消息时附带精确选中的行（文件、行号、代码块），无选区时至少附带当前打开的文件；
  - 回退到任意可见编辑器中仍存在的非空选区。

## [0.6.5] - 2026-08-14

### 新增

- 发送消息时自动附带活动编辑器的选中内容（Claude Code 同款）：选中一行/一段后直接在聊天里说「把这行替换成 …」，agent 就能看到文件路径、行号与代码块，无需手动粘贴。

## [0.6.4] - 2026-08-14

### 重构

- 移除插件侧模型自动路由：模型选择器变为 DSH 的纯索引层，列表、继承、effort 档位全部直通 DSH 原版行为。

## [0.6.3] - 2026-08-14

### 新增

- 模型选择器完全镜像 DSH Web UI：按 provider 分组的目录、每个模型声明的 effort 档位、DSH 侧继承（选中一次成为部署默认值）。

## [0.6.2] - 2026-08-14

### 修复

- 权限选择器按会话独立跟踪；修复 toast 元素显示问题。

## [0.6.1] - 2026-08-14

### 新增

- 模型 + 思考强度（effort）选择器、权限徽章点击选择与 Shift+Tab 循环切换、`@` 文件引用与 `/` 斜杠命令输入弹窗。

## [0.1.0] - 2026-08-14

### 新增

- DSH Chat for VS Code 首个版本：编辑器窗口形态的 DSH 会话工作区（OpenCode 式），连接运行中的 DSH 实例（HTTP RPC + WebSocket 双事件流），复用其全部 agent 能力。
- 会话历史、问题卡片、权限模式、会话持久化等基础能力。
- Marketplace 发布指南与仓库元数据。

---

> 说明：0.2.0 – 0.6.0 为早期迭代打包版本，未在 git 中留下独立提交记录，故此处不逐一列出变更；可追溯的提交自 0.6.1 起。
